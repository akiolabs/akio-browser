export const cookie = {
  get({key}) {
    // TODO
  },
  update({key, value}) {
    // TODO
  },
};

export const localStorage = {
  get({key}) {
    // TODO
  },
  update({key, value}) {
    // TODO
  },
};

export const noStorage = {
  get({key}) {
    // noop
  },
  update({key, value}) {
    // noop
  },
};
